Placeholder walker pseudo-code
# real Jac code will be required in your Jaseci environment


walker parse_resume(payload: dict){
# payload.resume_text
# parse resume, extract skills -> create nodes + edges
}